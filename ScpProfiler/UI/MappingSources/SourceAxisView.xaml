<UserControl
    x:Class="ScpProfiler.Views.MappingSources.SourceAxisView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:scpProfiler="clr-namespace:ScpProfiler"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=gamepad:SourceAxisViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="60" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <!-- Button Icon -->
        <Image
            Grid.Column="0" Grid.Row="0"
            ToolTip="{Binding Path=Name}"
            Width="48"
            Source="{Binding IconSource}" />
        <!-- Button/Axis signal strength -->
        <ProgressBar
            Grid.Column="1" Grid.Row="0"
            ToolTip="Signal strength"
            VerticalAlignment="Top"
            HorizontalAlignment="Stretch"
            Height="16" Margin="5,16"
            Value="{Binding Value, Mode=OneWay}" />
        <Label
            Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"
            Content="{Binding Name, FallbackValue=Unbinded}" />
    </Grid>
</UserControl>